import { useState } from "react";

const Onboarding = () => {
  const [step, setStep] = useState<number>(0);

  return <section className="max-w-2xl w-full m-auto bg-red-500">
    
  </section>;
};

export default Onboarding;
